<ContentDialog
    x:Class="LumaCompanion.WinUI.Views.LogViewDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LumaCompanion.WinUI.Views"
    xmlns:models="using:LumaCompanion.WinUI.Models" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Conversation Log Viewer"
    CloseButtonText="Close"
    DefaultButton="Close"
    Background="{StaticResource LumaDarkGreyBrush}"
    Foreground="{StaticResource LumaWhiteBrush}"
    MinWidth="600" MinHeight="400">

    <Grid>
        <ListView x:Name="LogListView" 
                  ItemsSource="{x:Bind LogEntries}"
                  Background="{StaticResource LumaLightGreyBrush}"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  SelectionMode="None">
            <ListView.Header>
                <TextBlock Text="Conversation Entries" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="{StaticResource LumaWhiteBrush}"/>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:LogEntry">
                    <StackPanel Margin="5" Padding="10" Background="{StaticResource LumaDarkGreyBrush}" CornerRadius="5">
                        <TextBlock FontSize="12" Foreground="LightGray">
                            <Run Text="Timestamp:"/>
                            <Run Text="{x:Bind Timestamp, Converter={StaticResource DateTimeFormatConverter}, ConverterParameter='yyyy-MM-dd HH:mm:ss UTC'}" FontWeight="SemiBold"/>
                        </TextBlock>
                        <TextBlock TextWrapping="WrapWholeWords" Margin="0,5,0,0">
                            <Run Text="User:" FontWeight="Bold" Foreground="{StaticResource LumaOrangeBrush}"/>
                            <Run Text="{x:Bind UserInput}"/>
                        </TextBlock>
                        <TextBlock TextWrapping="WrapWholeWords" Margin="0,5,0,0">
                            <Run Text="Luma:" FontWeight="Bold" Foreground="{StaticResource LumaOrangeBrush}"/>
                            <Run Text="{x:Bind AiResponse}"/>
                        </TextBlock>
                        <Grid Margin="0,5,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0">
                                <Run Text="Sentiment:" Foreground="LightGray"/>
                                <Run Text="{x:Bind Sentiment}" FontWeight="SemiBold"/>
                            </TextBlock>
                            <TextBlock Grid.Column="2" Margin="10,0,0,0">
                                <Run Text="Flagged:" Foreground="LightGray"/>
                                <Run Text="{x:Bind Flagged}" FontWeight="SemiBold"/>
                            </TextBlock>
                        </Grid>
                        <!-- Optional: Display scores if needed, could be in a flyout or expandable section -->
                        <!-- 
                        <Expander Header="Moderation Scores" Margin="0,5,0,0" FontSize="12">
                            <StackPanel Margin="10,5,0,0" Spacing="3">
                                <TextBlock><Run Text="Self-Harm:" Foreground="Gray"/> <Run Text="{x:Bind SelfHarmScore}"/></TextBlock>
                                <TextBlock><Run Text="Violence:" Foreground="Gray"/> <Run Text="{x:Bind ViolenceScore}"/></TextBlock>
                                <TextBlock><Run Text="Hate:" Foreground="Gray"/> <Run Text="{x:Bind HateScore}"/></TextBlock>
                                <TextBlock><Run Text="Harassment:" Foreground="Gray"/> <Run Text="{x:Bind HarassmentScore}"/></TextBlock>
                            </StackPanel>
                        </Expander>
                        -->
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="0,0,0,5"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
        <TextBlock x:Name="NoLogsTextBlock" 
                   Text="No conversation logs found." 
                   Visibility="Collapsed" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                   FontSize="18" Foreground="Gray"/>
    </Grid>
    <ContentDialog.Resources>
        <local:DateTimeFormatConverter x:Key="DateTimeFormatConverter"/>
    </ContentDialog.Resources>
</ContentDialog>
